---
"cc-wf-studio": patch
"@cc-wf-studio/cli": patch
---

Add "Group selection" to the canvas context menu (Ctrl/Cmd+G): wraps the selected nodes in a new, correctly-sized group in one step — single undo entry — instead of creating a group from the palette, resizing it, and dragging each node in by hand.
