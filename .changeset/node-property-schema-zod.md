---
"@cc-wf-studio/core": minor
"cc-wf-studio": patch
---

Add zod-based, target-scoped node property schemas (foundation + subAgent). The SubAgent property panel now scopes Claude Code-only fields from the shared schema instead of hard-coded conditionals, and core exposes helpers to derive "field ignored by target" export warnings.
