---
"cc-wf-studio": minor
---

Add one-click Auto Layout to the canvas toolbar: arranges the whole workflow into a tidy left-to-right layered layout — groups are laid out internally and resized to fit their children — then re-fits the view. The whole operation is a single undo step.
