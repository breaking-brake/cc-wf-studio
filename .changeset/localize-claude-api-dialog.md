---
"cc-wf-studio": patch
---

Localize all visible text in the Claude API upload dialog (API-key screens, skill list, upload confirmation, success/error screens, MCP auth guidance, and the API Test chat) in all 5 UI languages — and fix a validation message that was hardcoded Japanese for every locale.
