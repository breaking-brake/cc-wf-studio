{
  "name": "@cc-wf-studio/electron",
  "version": "3.26.0",
  "private": true,
  "license": "AGPL-3.0-or-later",
  "main": "dist/main/main.js",
  "scripts": {
    "build": "echo 'Electron build requires: npm install electron' && tsc -p tsconfig.main.json && tsc -p tsconfig.preload.json",
    "start": "electron dist/main/main.js",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@cc-wf-studio/core": "*"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "@types/node": "^25.0.3",
    "electron": "^33.0.0"
  }
}
